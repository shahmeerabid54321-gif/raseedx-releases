# RaseedX releases

Android APK downloads for the RaseedX in-app updater.
The application source lives in a private repository; only signed release
binaries are published here so devices can fetch updates without a login.

Each release tag matches the app version. The APK filename carries the
Android versionCode, which is what the in-app updater actually compares.
